
WITH RT as (SELECT distinct trim(x.EXTCLM) AS EXTCLM ,
(d.BRANCD||right(d.BATDAT,4)||SUBSTR(d.BATDAT,2,2)||LPAD(d.BATSEQ,3,'0')||LPAD(d.SEQNUM,2,'0')) || LPAD(d.LINENO,3,'0') as RT_ReportKey, 
(d.BRANCD||right(d.BATDAT,4)||SUBSTR(d.BATDAT,2,2)||LPAD(d.BATSEQ,3,'0')||LPAD(d.SEQNUM,2,'0')) as RT_CLAIM_ID
,LPAD(d.LINENO,3,'0')AS RT_CLAIM_LINE
,d.SVCCOD,d.ALWAMT,'' as ALL_HOLDCDS,
(SELECT distinct HOLDCD FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=1
FETCH FIRST 1 ROWS ONLY
)AS HOLDCD1 
,
(SELECT distinct HOLDCD FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=2
FETCH FIRST 1 ROWS ONLY
)AS HOLDCD2
,
(SELECT distinct HOLDCD FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=3
FETCH FIRST 1 ROWS ONLY
)AS HOLDCD3 
,
(SELECT distinct HOLDCD FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=4
FETCH FIRST 1 ROWS ONLY
)AS HOLDCD4
,
(SELECT distinct HOLDCD FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=5
FETCH FIRST 1 ROWS ONLY
)AS HOLDCD5
,
(SELECT distinct HOLDCD FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=6
FETCH FIRST 1 ROWS ONLY
)AS HOLDCD6
,
d.FEECOD ,  d.ALWAMT AS ALLOWED_AMT,d.claamt AS CHARGES
,d.COMPNO,d.clatyp,
d.POSCOD,d.benpkg,d.PROVNO,p.PRVTYP ,trim(p.LSTNAM) ||' '|| trim(p.FSTNAM) AS CONTRACT_NAME, 
p.FEDNUM AS TIN, d.TOPAMT,d.DEDAMT, d.COPAMT,d.COIAMT,d.OOPAMT, '' as ignore ,d.CPTCOD,'' AS ALL_MOD, d.MODCOD, d.MODCD2, d.MODCD3,d.CPTMD1, d.CPTMD2, d.CPTMD3
,(SELECT DESCRP FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=1
FETCH FIRST 1 ROWS ONLY
) AS HOLD_DESC_1
,(SELECT distinct DESCRP FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=2
FETCH FIRST 1 ROWS ONLY
) AS HOLD_DESC_2
,(SELECT distinct DESCRP FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=3
FETCH FIRST 1 ROWS ONLY
) AS HOLD_DESC_3
,(SELECT distinct DESCRP FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=4
FETCH FIRST 1 ROWS ONLY
) AS HOLD_DESC_4
,(SELECT distinct DESCRP FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=5
FETCH FIRST 1 ROWS ONLY
) AS HOLD_DESC_5
,(SELECT distinct DESCRP FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=6
FETCH FIRST 1 ROWS ONLY
) AS HOLD_DESC_6

,(SELECT distinct HLDSRC FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=1
FETCH FIRST 1 ROWS ONLY
) AS HOLD_SRC_1
,(SELECT distinct HLDSRC FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=2
FETCH FIRST 1 ROWS ONLY
) AS HOLD_SRC_2
,(SELECT distinct HLDSRC FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=3
FETCH FIRST 1 ROWS ONLY
) AS HOLD_SRC_3
,(SELECT distinct HLDSRC FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=4
FETCH FIRST 1 ROWS ONLY
) AS HOLD_SRC_4
,(SELECT distinct HLDSRC FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=5
FETCH FIRST 1 ROWS ONLY
) AS HOLD_SRC_5
,(SELECT distinct HLDSRC FROM MHSFLP.EXHMAS WHERE BRANCD=d.BRANCD AND BATDAT=d.BATDAT AND BATSEQ=d.BATSEQ AND CLMSEQ =d.SEQNUM AND LINENO=d.LINENO 
AND TRNSEQ=6
FETCH FIRST 1 ROWS ONLY
) AS HOLD_SRC_6

,x.edibat,d.FORMCD,x.STATCD as ClaimStatus,d.BRANCD,d.BATDAT,
d.BATSEQ,d.SEQNUM,d.LINENO,d.statcd as LineStatus,d.LOBCOD,d.MEMBNO,'' as empty1,'' as empty2,'' as empty3 , '' as empty4,x.clmfrq, d.BENCOD, 
d.SVCDAT, d.ENDDAT, x.DIAGcd001,d.PRVCPY,d.athbch,d.athdat,d.athbat,d.athseq,d.athlin,
d.SYSDAT, d.EDTDAT, d.PSTDAT
,d.UNITCT,
(Case when FORMCD ='U' then 'Facility Claim' when FORMCD ='H' then 'Professional Claim' else 'Error' end) as ClaimType

,(select distinct Descrp from MHSFLP.GNRRF1MT where REFTYP='SP' AND REFVAL in 
(
SELECT distinct AA.SPCCOD FROM (select provno,SPCCOD, row_number()over (partition by provno order by SPCCOD) rn
from MHSFLP.PSHMAS )AA WHERE AA.provno=d.provno and AA.rn=1 
)
)
as Specialty_1,
(select distinct Descrp from MHSFLP.GNRRF1MT where REFTYP='SP' AND REFVAL in 
(
SELECT distinct AA.SPCCOD FROM (select provno,SPCCOD, row_number()over (partition by provno order by SPCCOD) rn
from MHSFLP.PSHMAS )AA WHERE AA.provno=d.provno and AA.rn=2 
)
)
as Specialty_2,
(select distinct Descrp from MHSFLP.GNRRF1MT where REFTYP='SP' AND REFVAL in 
(
SELECT distinct AA.SPCCOD FROM (select provno,SPCCOD, row_number()over (partition by provno order by SPCCOD) rn
from MHSFLP.PSHMAS )AA WHERE AA.provno=d.provno and AA.rn=3 
)
)
as Specialty_3, d.PREAMT, CASE WHEN FORMCD ='U' then concat(trim(m.FACTYP), x.clmfrq) ELSE '' END BILLTYPE, p.PARENT, p.PCCCOD, k.prvnpi, d.rcvdat
FROM (MHSFLP.MEDMAS AS d JOIN (MHSFLP.MEXMAS as x JOIN MHSFLP.CLMBCHET AS c ON x.EDIBAT=c.EDIBAT)
ON d.BRANCD=x.BRANCD AND d.BATDAT=x.BATDAT AND d.BATSEQ=x.BATSEQ AND d.SEQNUM=x.SEQNUM)

LEFT OUTER JOIN MHSFLP.PRVMAS AS p
on d.provno=p.provno

LEFT OUTER JOIN MHSFLP.APIMAS AS k
on d.provno=k.provno

LEFT OUTER JOIN MHSFLP.MXCMAS AS m
on d.BRANCD=m.BRANCD AND d.BATDAT=m.BATDAT AND d.BATSEQ=m.BATSEQ AND d.SEQNUM=m.SEQNUM

where x.EXTCLM in
('S72928978459','S2604111341318689722')
)
select distinct * from  RT
where BATDAT > '2260510'
ORDER BY EXTCLM,BATSEQ, SEQNUM,LINENO